HYDRA_HTTP=http://127.0.0.1:8443

# Must match the token HydraDB was started with (GRAPH_AUTH_TOKEN_FILE).
HYDRA_TOKEN=

HYDRA_NAMESPACE=default
HYDRA_GRAPH=default

# cookie/session auth to leak, but tighten this once you have a fixed domain).
CORS_ALLOW_ORIGINS=https://groundz.vercel.app/
