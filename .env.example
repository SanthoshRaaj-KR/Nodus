# Backend (blastradius-api / ui.server) -- how it reaches HydraDB, and who
# it will accept cross-origin browser requests from.
#
# Copy to .env and fill in real values: `cp .env.example .env`
# Never commit .env -- it's gitignored on purpose.

# Where HydraDB is reachable from this process.
#   Local dev (HydraDB via `python -m blastradius.cli up`):
HYDRA_HTTP=http://127.0.0.1:8443
#   Hosted (docker-compose, HydraDB reached by its service name):
#   HYDRA_HTTP=http://hydradb:8443

# Must match the token HydraDB was started with (GRAPH_AUTH_TOKEN_FILE).
#   Local dev: the value `blastradius.cli up` seeds automatically -- see
#   hydradb-data/auth-token after running it.
#   Hosted: generate one with `openssl rand -hex 32` and use the same value
#   for both HYDRA_TOKEN here and GRAPH_AUTH_TOKEN_FILE's contents on hydradb.
HYDRA_TOKEN=local-development-token-32-bytes

HYDRA_NAMESPACE=default
HYDRA_GRAPH=default

# Browser origin(s) allowed to call this API. Comma-separate for more than
# one; "*" allows any origin (fine here since there's no cookie/session auth
# to leak, but tighten this once you have a fixed domain).
#   Local dev (Vite dev server):
CORS_ALLOW_ORIGINS=http://localhost:5173
#   Hosted (Vercel):
#   CORS_ALLOW_ORIGINS=https://your-app.vercel.app

# -- Chat agent (bottom-right widget) --------------------------------------
# The one value it needs. Without it the chat routes answer 503 and say so;
# everything else in the project keeps working.
OPENAI_API_KEY=

# Mini-class models only, and it is enforced -- a full-size model is refused.
CHAT_MODEL=gpt-5.4-mini

# Reasoning effort. "low" is the default: the agent chooses between facts the
# tools already computed rather than thinking, and this is the single biggest
# lever on time-to-first-token.
CHAT_REASONING_EFFORT=low

# Ceiling on tool-call rounds per question, so one bad question cannot turn
# into a thirty-second answer.
CHAT_MAX_TURNS=6

# ---------------------------------------------------------------------------
# Publishing a built graph
#
# Build locally, then hand the result to S3 without a second command: naming a
# bucket is enough, and the upload becomes the last stage of the ingest. Leave
# SNAPSHOT_BUCKET unset and every build stays on this machine.
#
# Credentials are not configured here. boto3 reads the standard chain --
# AWS_ACCESS_KEY_ID / AWS_SECRET_ACCESS_KEY, AWS_PROFILE and ~/.aws, or the
# instance role on a host that has one -- so the only thing this file needs to
# say is *where* the archive goes.
# ---------------------------------------------------------------------------
# SNAPSHOT_BUCKET=your-bucket
# SNAPSHOT_PREFIX=snapshots/
# SNAPSHOT_NAME=graph-latest.tar.gz
# SNAPSHOT_REGION=ap-south-2
# Set to 0 to keep a configured bucket but publish only on demand.
# SNAPSHOT_AUTO_PUSH=1
